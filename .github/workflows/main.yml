name: Deploy Website on Laptop

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Get Code
        uses: actions/checkout@v4

      - name: Deploy Website
        run: |
          echo "Deploying on your laptop..."
          start index.html
